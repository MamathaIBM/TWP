import { StAdminImplementPhaseActivityListModule } from './st-admin-implement-phase-activity-list.module';

describe('StAdminImplementPhaseActivityListModule', () => {
  let stAdminImplementPhaseActivityListModule: StAdminImplementPhaseActivityListModule;

  beforeEach(() => {
    stAdminImplementPhaseActivityListModule = new StAdminImplementPhaseActivityListModule();
  });

  it('should create an instance', () => {
    expect(stAdminImplementPhaseActivityListModule).toBeTruthy();
  });
});
